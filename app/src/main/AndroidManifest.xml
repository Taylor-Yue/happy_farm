<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.happy_farm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FindFragmentItemActivity"></activity>
        <activity android:name=".registerPage" />
        <activity
            android:name=".ui.login.registerPage"
            android:label="@string/title_activity_register_page" />
        <activity android:name=".the_root_page_activity" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@drawable/btn_bg_pressed" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>